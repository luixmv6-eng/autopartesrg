# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# La plantilla sí se versiona: no lleva ningún valor real (el hash del panel es
# un marcador y el secreto de sesión va vacío) y el README manda copiarla con
# `cp .env.example .env.local`, que sin esto falla en un clon recién hecho.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
datos/
